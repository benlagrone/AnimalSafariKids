version: "3.9"

services:
  animalsafarikids:
    build:
      context: .
    image: animalsafarikids:latest
    container_name: animalsafarikids
    env_file:
      - .env
    volumes:
      - ./:/app
    networks:
      - fortress-phronesis-net

networks:
  fortress-phronesis-net:
    external: true
    name: fortress-phronesis-net
